pipeline {
    agent any
    parameters {
        choice(name: 'language', choices: ['ALL', 'C', 'JAVA','PYTHON'], description: 'language choice')
    }
    stages {
        stage('build') {
            steps {
                sh '''
                ./jenkins.sh
                echo "$language"
                echo "hello world"
                ls -la
                '''

            }
        }
    }
}
